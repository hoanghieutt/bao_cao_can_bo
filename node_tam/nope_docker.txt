*************** docker// dùng để build và run các container tương đương như  docker rundocker-compose scale <tên service> = <số lượng>// Danh sách tất cả container (bao gồm đã dừng): viết tắt là process status -Alldocker ps -a // dừng container:docker stop my_container// xoá containerdocker rm my_container*******/docker image// tải image từ docker hubdocker pull nginx// danh sách các image ************** docker Volume //cách lưu trữ dữ liệu bên ngoài containerdocker volume create <tên của mục cần lưu>vd :docker volume create todo// Sử dụng Docker Volume trong Containerdocker run -d --name my_container -v todo:/app/data ubuntu// xoá docker volume docker volume rm todo